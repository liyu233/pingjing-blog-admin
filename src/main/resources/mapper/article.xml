<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiandisifang.mapper.ArticleMapper">


    <select id="getArticle"  resultType="java.util.HashMap">
        select * from article where article_state = "1"
    </select>
    <!--<update id="updateArticleContent">
        update article set
        article_content_html = #{obj.articleContentHtml}
        where id = #{obj.id}
    </update>-->
</mapper>